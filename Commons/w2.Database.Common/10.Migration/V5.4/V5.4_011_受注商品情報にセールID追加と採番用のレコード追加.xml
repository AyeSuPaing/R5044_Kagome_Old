<?xml version="1.0" encoding="utf-8" ?>
<Migration>
  <Up>
    <Statement>
      <![CDATA[
ALTER TABLE [w2_OrderItem] ADD [productsale_id] [nvarchar] (10) NOT NULL DEFAULT (N'');
GO

declare @saleid bigint
select @saleid = CONVERT(bigint, ISNULL(MAX(productsale_id),0))
from w2_ProductSale

INSERT INTO w2_Number (dept_id, number_code, number, date_created, date_changed)
     VALUES ('0', 'ProductSaleId', @saleid, GETDATE(), GETDATE())
GO
      ]]>
    </Statement>
  </Up>
</Migration>