<?xml version="1.0" encoding="utf-8" ?>
<Migration>
  <Up>
    <Statement>
      <![CDATA[
        IF NOT EXISTS (SELECT * FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'w2_CsIncidentWarningIcon')
        BEGIN
        CREATE TABLE dbo.w2_CsIncidentWarningIcon (
          dept_id nvarchar (30) NOT NULL,
          operator_id nvarchar (20) NOT NULL,
          incident_status nvarchar (10) NOT NULL,
          warning_level nvarchar (1) NOT NULL,
          term int,
          date_created datetime NOT NULL DEFAULT (getdate()),
        last_changed nvarchar (20) NOT NULL DEFAULT (N'')
        )
        END
        GO

        IF NOT EXISTS (SELECT * FROM INFORMATION_SCHEMA.KEY_COLUMN_USAGE WHERE TABLE_NAME = 'w2_CsIncidentWarningIcon')
        BEGIN
          ALTER TABLE w2_CsIncidentWarningIcon ADD CONSTRAINT PK_w2_CsIncidentWarningIcon PRIMARY KEY CLUSTERED(
            dept_id,
            operator_id,
            incident_status,
            warning_level
          );
        END
        GO
      ]]>
    </Statement>
  </Up>
</Migration>